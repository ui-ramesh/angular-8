import { ColorExampleDirective } from './color-example.directive';

describe('ColorExampleDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorExampleDirective();
    expect(directive).toBeTruthy();
  });
});
